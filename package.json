{
  "name": "kitchen-sink",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm generate-sdks && turbo run build",
    "build:watch": "pnpm generate-sdks && turbo run build:watch",
    "check-types": "pnpm generate-sdks && turbo run check-types",
    "generate-api-sdk": "turbo run generate-sdk",
    "generate-websocket-models": "turbo run generate-websocket-models",
    "generate-websocket-sdk": "turbo run generate-websocket-sdk",
    "generate-sdks": "turbo run generate-websocket-models generate-websocket-sdk generate-api-sdk",
    "clean": "turbo run clean && rm -rf .turbo dist node_modules/.cache packages/*/.turbo packages/*/dist apps/*/.turbo apps/*/dist",
    "db:logs": "docker compose -f docker-compose.dev.yaml logs -f",
    "db:shell": "docker compose -f docker-compose.dev.yaml exec db psql -U ${DB_USER:-root} -d ${DB_DATABASE:-database}",
    "db:start": "docker-compose -f docker-compose.dev.yaml up -d",
    "db:stop": "docker compose -f docker-compose.dev.yaml down",
    "dev": "pnpm db:start && turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test": "turbo run test -- --coverage"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.7.4",
    "turbo": "^2.7.3"
  },
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@asyncapi/cli": "^5.0.5",
    "lint-staged": "^16.2.7"
  }
}
